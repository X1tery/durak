#pragma once
#include <chrono>
#include <unordered_set>
#include <string>

extern size_t PLR_COUNT;
extern const size_t MAX_NAME_SIZE;
extern const char* SERV_PORT;
extern const size_t BUFF_SIZE;
extern const std::chrono::milliseconds UPD_CLOCK;
extern const std::unordered_set<std::string> ACTIONS;
extern const std::string HELP_MSG;